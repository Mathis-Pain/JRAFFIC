# Makefile

Le Makefile automatise la compilation et l'exécution du programme. Il évite de retaper les commandes à chaque lancement : il suffit d'exécuter make dans le terminal.

make clean pour repartir a zero
